import React from 'react';
import ChatSidebar from './ChatSidebar';

const ChatPage = () => {
  return (
    <div className="flex-1 bg-slate-900 h-screen">
      <ChatSidebar />
    </div>
  );
};

export default ChatPage; 